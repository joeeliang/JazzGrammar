services:
  - type: web
    name: jazz-grammar-api
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn api_server:app --app-dir backend --host 0.0.0.0 --port $PORT
    envVars:
      - key: CORS_ALLOW_ORIGINS
        value: "*"
